<div class="{{if red 'red-input'}}">
  {{#if (eq type 'input')}}
    {{md-input type="text" value=value label=label }}
  {{else if (eq type "textarea")}}
  <div class="input-field">
    {{textarea class="textarea-autocomplete" value=value label=label}}
    <label class="textarea-label-{{label}}" >{{label}}</label>
  </div>
  {{/if}}
</div>
